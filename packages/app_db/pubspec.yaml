name: app_db
description: "Drift/SQLite schema for feeds, feed sources, read marks, watched channels and settings."
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  drift: ^2.35.0
  path: ^1.9.1
  sqlite3: ^3.6.0
  # none yet

dev_dependencies:
  build_runner: ^2.16.1
  drift_dev: ^2.35.0
  lints: ^6.0.0
  test: ^1.25.6
